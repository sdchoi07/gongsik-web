spring:
  profiles:
    active: local
  security:
      user:
       name: user
       password: 1111
  data:
   rest:
    base-path: /api
  thymeleaf:
    prefix: classpath:/templates/
    suffix: .html
logging:
   level:
     com:
     gongsik:
     gsw: DEBUG